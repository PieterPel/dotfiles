name: pre-commit

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    
    steps:
      - uses: ./.github/workflows/setup-nix.yml
      - run: pre-commit run --all-files

